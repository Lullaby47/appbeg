const { cert, getApps, initializeApp } = require('firebase-admin/app');
const { getFirestore } = require('firebase-admin/firestore');
const { Pool } = require('pg');
const args = new Map(process.argv.slice(2).map((a,i,arr)=>a.startsWith('--')?[a.split('=')[0],a.includes('=')?a.split('=').slice(1).join('='):(arr[i+1]?.startsWith('--')?'true':arr[i+1]||'true')]:[a,'']));
const DRY_RUN=args.has('--dry-run'), ONLY_MISSING=args.has('--only-missing'), LIMIT=Number(args.get('--limit')||0)||0;
const clean=v=>String(v||'').trim(); function req(n){const v=clean(process.env[n]); if(!v) throw new Error(`${n} is required`); return v;}
function init(){const s=JSON.parse(Buffer.from(req('FIREBASE_SERVICE_ACCOUNT_BASE64'),'base64').toString('utf8')); return getApps()[0]||initializeApp({credential:cert(s)});}
function dt(v){if(!v)return null; if(v instanceof Date)return v.toISOString(); if(typeof v==='string')return v; if(typeof v.toDate==='function')return v.toDate().toISOString(); if(typeof v.toMillis==='function')return new Date(v.toMillis()).toISOString(); if(typeof v.seconds==='number')return new Date(v.seconds*1000).toISOString(); return null;}
function json(v){if(v==null)return null; if(v instanceof Date)return v.toISOString(); if(Array.isArray(v))return v.map(json); if(typeof v==='object'){const d=dt(v); if(d)return d; return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,json(x)]));} return v;}
async function main(){init(); const db=getFirestore(); const pg=new Pool({connectionString:clean(process.env.DATABASE_URL||process.env.POSTGRES_URL)||req('DATABASE_URL')}); const existing=ONLY_MISSING?new Set((await pg.query('SELECT code FROM public.referral_codes_cache WHERE deleted_at IS NULL')).rows.map(r=>String(r.code))):new Set(); let q=db.collection('referralCodes'); if(LIMIT)q=q.limit(LIMIT); const snap=await q.get(); let would_upsert=0,upserted=0,errors=0; for(const doc of snap.docs){if(existing.has(doc.id))continue; would_upsert++; if(!DRY_RUN)try{const d=doc.data()||{}; await pg.query(`INSERT INTO public.referral_codes_cache (code,player_uid,created_at,raw_firestore_data,source,mirrored_at,deleted_at) VALUES ($1,NULLIF($2,''),$3::timestamptz,$4::jsonb,'firebase_backfill',now(),NULL) ON CONFLICT (code) DO UPDATE SET player_uid=EXCLUDED.player_uid,created_at=COALESCE(public.referral_codes_cache.created_at,EXCLUDED.created_at),raw_firestore_data=EXCLUDED.raw_firestore_data,source=EXCLUDED.source,mirrored_at=now(),deleted_at=NULL`,[doc.id,clean(d.playerUid),dt(d.createdAt),JSON.stringify(json(d)||{})]); upserted++;}catch(e){errors++; console.error('[BACKFILL_REFERRAL_CODES_CACHE] failed',{code:doc.id,error:e});}} await pg.end(); console.log(JSON.stringify({collection:'referralCodes',firebase_count_seen:snap.size,would_upsert,upserted,errors},null,2));}
main().catch(e=>{console.error('[BACKFILL_REFERRAL_CODES_CACHE] fatal',e);process.exitCode=1;});
